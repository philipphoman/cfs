#-----------------------------------------------------------------------
# Reversal learning CFS
#
# Load and prepare data
#-----------------------------------------------------------------------
# PH, 3/23/18
#-----------------------------------------------------------------------
source("../src/cfs_func.R")
#-----------------------------------------------------------------------
# read data frame
df <- read.csv("../data/cfs.csv")
# Restrict to complete baseline cases
#df <- df %>% filter(!is.na(stait), !is.na(stais), !is.na(fsq))
#-----------------------------------------------------------------------
